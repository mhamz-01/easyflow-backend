# Notes

1. FK column exists → belongsTo
2. One-to-many → belongsTo + hasMany
3. Many-to-many → belongsToMany + join table
4. targetKey → only when NOT referencing id
5. hasMany → NEVER creates columns
